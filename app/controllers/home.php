<?php
/**
* Home controller
* Uses core Controller's model Method to get an instance
* of the model it wnats to use. In this case the User model.
* and the view Method to pass the data to
**/

class Home extends Controller 
{
    public function index($name = '')
    {
        //get an instance of a model by passing the name of a model e.g 'User' to model method

    /**
     * pass the path of the view  and data as an array to the
     * view method  of core Controller.
     * view method will require the view for us
    **/

    }
}







